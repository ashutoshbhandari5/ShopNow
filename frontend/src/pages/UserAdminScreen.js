import React from "react";
import UserScreen from '../Admin/UsersScreen/userScreen'

const UserAdminScreen = ({ history }) => {
  return (
    <>
      <UserScreen history={history} />
    </>
  );
};

export default UserAdminScreen;
