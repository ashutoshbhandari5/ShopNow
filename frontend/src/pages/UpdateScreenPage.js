import React from "react";
import UpdateScreen from "../UserProfileUpdate/updateScreen";

const UpdateScreenPage = ({ history }) => {
  return (
    <>
      <UpdateScreen history={history} />
    </>
  );
};

export default UpdateScreenPage;
